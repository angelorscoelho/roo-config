{
  "_doc": "Project-level MCP config. Commit this file. Zero credentials. Overrides global mcp_settings.json for listed servers.",
  "mcpServers": {
    "serena": {
      "command": "uvx",
      "args": [
        "--from", "git+https://github.com/oraios/serena",
        "serena", "start-mcp-server",
        "--context", "ide-assistant",
        "--project-from-cwd",
        "--no-open-browser"
      ]
    },
    "ref.tools": {
      "_doc": "DOCS: Token-efficient doc lookups. 95% fewer tokens than Context7. Requires REF_TOOLS_API_KEY.",
      "type": "streamable-http",
      "url": "https://api.ref.tools/mcp",
      "headers": {
        "X-ref-API-Key": "${env:REF_TOOLS_API_KEY}"
      }
    }
  }
}
